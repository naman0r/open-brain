# Absolute or ~-relative path to the Obsidian context vault
VAULT_ROOT=~/Developer/context-vault

# Commit every agent write so a bad write is one `git revert` away
VAULT_AUTO_COMMIT=true

# Bearer token for the HTTP transport. Unused by stdio.
OPEN_BRAIN_API_TOKEN=change-me
